# Safe configuration examples

This directory contains non-secret configuration examples only.

Secrets, credentials, tokens, runtime datasets, backups and protected state must not be committed here.
